//! Playback stage. Filled in a later task.
use crate::SpriteStudio;
use gpui::{div, prelude::*, Context, Window};

pub fn render(_app: &mut SpriteStudio, _window: &mut Window, _cx: &mut Context<SpriteStudio>) -> impl IntoElement {
    div()
}
